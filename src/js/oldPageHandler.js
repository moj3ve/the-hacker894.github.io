      function resetPage() {
/*	
        $(`.turnPhoneContainer`).fadeOut(500)
        $(`.screenSmall`).fadeOut(500)

        $(`.p2`).fadeOut(500)
          $(`.p3`).fadeOut(500)
          $(`.p1`).fadeIn(500)

          $(`.pages`).fadeIn(500)
          $(`.pages-selector`).fadeIn(500)
          $(`div.pages-selector`).fadeIn(500)
          $(`.pages-selection`).addClass("show")
          $(`.pages-selector`).addClass("show")

          $(`div.left.column`).fadeIn(500)
          $(`div.right.column`).fadeIn(500)
          $(`.left`).fadeIn(500)
          $(`.right`).fadeIn(500)
          $(`.pages-selection`).fadeIn(500)
          $(`.pages-loading`).fadeOut(500)
          

          $('.selector-1').css({opacity: 1.0})
          $('.selector-2').css({opacity: 0.1})
          $('.selector-3').css({opacity: 0.1})
          */
          return;
      }
      function checkTooSmall() {
      /*  
          $(`.pages`).fadeOut(500)
          $(`div.pages-selector`).fadeOut(500)
          $(`div.left.column`).fadeOut(500)
          $(`div.right.column`).fadeOut(500)
          $(`.left`).fadeOut(500)
          $(`.right`).fadeOut(500)
          $(`div.pages-selection`).fadeOut(500)
          $(`.pages-loading`).fadeOut(500)

          $(`.screenSmall`).fadeIn(500)
          if(orientation == 0) {
            // If device is in portrait
            $(`.turnPhoneContainer`).fadeIn(500)

            
          }
          if(orientation == 180) {
            // If device is in portrait (upside down)
            $(`.turnPhoneContainer`).fadeIn(500)

			            
          }
          if(parseInt($(window).width()) < parseInt($(window).height())) {
            // If window width is less than height
            // This usally indicates portrait mode
            $(`.turnPhoneContainer`).fadeIn(500)

          }
          if(parseInt($(window.width()) <= 640)) {
          	$(`.screenSmall`).fadeIn(500)
          } else return */ 
          return;
  			

        
        console.log("Checking")
        
      }